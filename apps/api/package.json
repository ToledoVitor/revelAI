{
  "name": "@revelai/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/clean-production-dist.mjs && tsc --project tsconfig.build.json && pnpm run check:production-dist",
    "check:production-dist": "node scripts/assert-production-dist.mjs",
    "lint": "eslint src",
    "typecheck": "tsc --project tsconfig.json",
    "test": "vitest run",
    "openapi:generate": "pnpm run build && node scripts/generate-openapi.mjs",
    "openapi:check": "pnpm run build && node scripts/generate-openapi.mjs --check",
    "benchmark:roboflow": "pnpm run build && node scripts/benchmark-roboflow-wall-pass.mjs",
    "demo:start": "pnpm --filter @revelai/config run build && pnpm run build && node scripts/start-local-demo.mjs",
    "demo:smoke": "pnpm --filter @revelai/config run build && pnpm run build && node scripts/start-local-demo.mjs --check",
    "operator:receipt-import": "pnpm --filter @revelai/config run build && pnpm run build && node scripts/import-benchmark-receipt.mjs",
    "operator:receipt-smoke": "pnpm --filter @revelai/config run build && pnpm run build && node scripts/import-benchmark-receipt.mjs --help"
  },
  "dependencies": {
    "@fastify/busboy": "3.2.2",
    "@revelai/config": "workspace:*",
    "@revelai/contracts": "workspace:*",
    "@revelai/domain": "workspace:*",
    "@revelai/vision": "workspace:*",
    "better-sqlite3": "13.0.3",
    "fastify": "5.12.1",
    "jpeg-js": "0.4.4",
    "zod": "4.1.12"
  },
  "devDependencies": {
    "@types/better-sqlite3": "9.6.0",
    "typescript": "5.9.3",
    "vitest": "3.2.4"
  }
}
