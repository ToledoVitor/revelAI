# Design QA — RevelAI web home

## Audit record

## W6 document authority

Only [Final W6 acceptance](#final-w6-acceptance--2026-09-03) is the current W6 authority. Every earlier W6 `pending`, `current authority`, `authoritative`, `current disposition`, reviewer disposition, and `final result` statement is a dated historical checkpoint retained as evidence; it is superseded by the independent Sol-approved hosted result at the end of this document.

- Audited route/state: `/`, `ready` / `home-default`.
- Source captures: `docs/design/references/desktop-home.png` (native `1487×1058`) and `docs/design/references/mobile-home.png` (native `853×1844`).
- Implementation captures: `apps/web/coverage/playwright/visual-artifacts/home-default--1440x1024--dpr-1--root--ready.png` and `apps/web/coverage/playwright/visual-artifacts/home-default--390x844--dpr-2--root--ready.png`.
- Desktop compare: source is normalized nearest-neighbour to `1440×1024` CSS pixels; the implementation capture is `1440×1024` CSS pixels at DPR `1`, with normalized pixel density `1`.
- Mobile compare: source is normalized nearest-neighbour to `390×844` CSS pixels; the implementation capture is `390×844` CSS pixels at DPR `2`, captured at CSS scale and normalized pixel density `1`.
- The source DPR is not embedded in the approved PNGs, so their native raster dimensions are recorded above; the comparison always uses the stated CSS dimensions and density-normalized files.

## Evidence reviewed

The full-view comparison input combines the approved reference and the implementation capture at equal dimensions:

- Desktop full: `apps/web/coverage/playwright/visual-artifacts/home-default--1440x1024--dpr-1--root--ready.overlay.png`.
- Mobile full: `apps/web/coverage/playwright/visual-artifacts/home-default--390x844--dpr-2--root--ready.overlay.png`.

The focused comparison input combines the same approved reference and implementation after only the variable runtime photo is hidden. It checks ink in the UI that sits over that photo:

- Desktop focused navigation: `apps/web/coverage/playwright/visual-artifacts/home-default--1440x1024--dpr-1--root--ready.ui-ink-overlay.png`.
- Mobile focused brand/headline/description: `apps/web/coverage/playwright/visual-artifacts/home-default--390x844--dpr-2--root--ready.ui-ink-overlay.png`.

The artifacts include each normalized reference, capture, overlay, diff, focused ink reference/capture/overlay/diff, and JSON metadata with viewport, DPR, route, state, fixture, CSS capture scale, and normalized density.

## Visual assessment

| Area               | Desktop 1440×1024                                                                                                                                                                                                                             | Mobile 390×844                                                                                                                                                 |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Typography         | Bundled Bebas Neue 400 provides the deliberately heavy condensed uppercase display treatment. Body copy uses bundled Arimo 400/700, an Arial-metric-compatible face that keeps the approved reference geometry stable across macOS and Linux. | Same bundled body and display faces; mobile scales and compresses the heading without truncation.                                                              |
| Layout and spacing | Header is 82px; photo begins at x=668 (46.4% width), matching the approved ~46% composition. Copy, decisions, and CTA remain within the warm-white panel.                                                                                     | Header is 58px; full ready state is exactly 844px tall. Brand, heading, description, both choices, and CTA all remain in viewport with no horizontal clipping. |
| Color and rules    | Shared tokens are warm white `#F7F5F0`, near black `#10110F`, deep emerald `#006B3C`, muted gray `#686B67`, and border gray `#CDD1CC`. Borders/rules are square and restrained, with no gradients.                                            | Same token palette; the mobile CTA uses deep emerald with warm-white type to preserve contrast over the compact decision area.                                 |
| Image and crop     | Approved runtime `public/assets/futsal-hero.png` is contained in the 53.6% right photo field with `object-fit: cover` / 57% horizontal crop. The source screenshot photo is intentionally different at runtime.                               | The same approved runtime asset occupies the upper 33.5rem field and remains intentionally variable from the source screenshot.                                |
| Copy and icons     | Approved visible Portuguese copy is present; unavailable destinations retain the exact unavailable message when activated. Choice and CTA arrows use `@phosphor-icons/react` `ArrowRight`.                                                    | Copy remains readable at 390px; the menu uses the approved Phosphor navigation icon rather than a custom glyph.                                                |

## Visual gate calibration

The full comparison excludes only the runtime-photo rectangle (desktop x=668 onward below the 82px header; mobile y=58 through y=535). The focused ink layer then hides that photo and compares UI over it.

- Image Pixelmatch threshold: `0.18`. The pinned Linux renderer baseline is 11.75% desktop and 14.26% mobile; the accepted limits remain 12.00% and 14.50%, respectively (0.25 and 0.24 percentage-point headroom).
- Focused UI Pixelmatch threshold: `0.10`. The same pinned baseline is 2.81% desktop and 23.19% mobile; limits remain 5.00% and 24.00% (2.19 and 0.81 percentage-point headroom).
- UI ink coverage retains the existing 90% rule, with independent versioned raster baselines: Darwin desktop navigation 416/374 pixels and mobile brand/headline/description 1040/936, 17232/15508, 2287/2058; pinned Linux desktop 165/148 and mobile 865/778, 15673/14105, 1578/1420.
- Negative proof in pinned Linux: hiding desktop navigation produces 0/148 navigation ink and is rejected; hiding mobile brand/headline/description produces 0/778, 0/14105, and 96/1420 ink and is rejected. The static source oracle is never derived from the same capture, so an already-missing normal control cannot establish its own floor.

### CI renderer architecture

`pnpm check` runs the browser’s structural checks on every supported host: font readiness, viewport bounds, console cleanliness, layout, interaction, keyboard, and reduced-motion behavior. Those scripts select their mode through the portable Node runner rather than shell environment-assignment syntax, and invoke the validated lifecycle `npm_execpath` through Node on every platform, so Windows still receives the structural suite without spawning a `.cmd` shim. Pixel comparison is explicit: local pixels require the named `darwin-arm64-local` renderer on Darwin/arm64, while CI pixels require exactly `playwright-1.62.1-noble-linux-amd64` on Linux/x64. Missing, unknown, mismatched, or host-incompatible modes fail before any pixel comparison; the harness receives that renderer identity as data and never infers a coverage baseline from `process.platform`.

The canonical CI step uses the immutable `mcr.microsoft.com/playwright@sha256:dcc5531e97840b9b5e794f2814476b21571c5124a3fca2267d73041f56e7580e` image with `--platform linux/amd64` and `--network none`. It mounts the pnpm/action-setup runtime read-only and invokes its pinned `pnpm.cjs`, avoiding a mutable image tag, Corepack activation, and a package-manager download in the visual container. This keeps pixel rasterization deterministic without loosening a threshold or masking any UI. Unsupported renderers receive the structural suite rather than an uncalibrated pixel baseline.

## Interaction and console checks

- Both approved viewports verify visible keyboard focus (3px outline) and Enter activation for every unavailable destination; focus lands on the unavailable heading.
- Mobile navigation remains keyboard-operable through its named toggle.
- The mobile `390×844` test asserts exact document height `844` and records no browser console warnings, console errors, or page errors.
- Unavailable interactions issue no follow-up browser requests after the initial asset/font load.

## W0 findings history

| Priority   | Finding                                                                                                                                     | Resolution                                                                                                              |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| P0 (fixed) | The pre-fix mobile page was 1919px high, leaving choices and CTA outside the 390×844 viewport.                                              | Responsive sizing was recalibrated and an exact-height/bounds Playwright test now protects the complete decision set.   |
| P1 (fixed) | The old visual mask covered mobile brand/headline/description and desktop navigation; 24–33% mismatch limits allowed deliberate UI removal. | Photo-only full mask is paired with focused UI ink diff and coverage floors; two deliberate removals now fail the gate. |
| P1 (fixed) | Desktop photo began at x=504 (35% width), invading the copy panel instead of the approved ~46% split.                                       | Hero width is 53.6%, moving the start to x=668; the desktop composition test accepts x=655–691.                         |
| P2         | No remaining P2 visual issue after the final full and focused comparisons.                                                                  | Not applicable.                                                                                                         |

## W6 initial acceptance record — historical

This was the initial, then-pending review checkpoint; its dispositions are historical and superseded by Final W6 acceptance.

### Capture matrix and inspection

All W6 captures were freshly generated on 2026-09-02, at `390×844`, DPR `2`, CSS capture scale, and normalized pixel density `1`. Each row below has a normalized reference, CSS-pixel capture, 50% overlay, pixel diff, and JSON metadata in `apps/web/coverage/playwright/visual-artifacts/`. No region is masked for these six non-home comparisons; the visual harness records the complete candidate UI. The existing W0 home-only photo mask and its pinned budgets remain unchanged.

| ID     | Route / exact state                 | Viewport / reference / fixture                                             | Capture evidence                                                                                                                                 | Severity | Evidence before                                                                                                                            | Change / fix                                                                                                                                    | Evidence after                                                                                                                            | Status / reviewer                                            |
| ------ | ----------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| W6-V01 | `/`, `ready`                        | `390×844`, DPR 2; `mobile-home.png`; `home-default`                        | `home-default--390x844--dpr-2--root--ready.{png,reference-normalized.png,overlay.png,diff.png,metadata.json}`                                    | P3       | Fresh full, unmasked comparison differs by `34.804%`; overlay shows the known approved-runtime hero/crop variance and typography geometry. | No W6 production change: the W0 home mask/ink gate remains immutable.                                                                           | Metadata and all four artifacts inspected; existing focused W0 gate remains the authority for the intentional runtime-photo difference.   | Disposition pending Sol confirmation; visual owner           |
| W6-V02 | `/verified`, `challenge-choice`     | `390×844`, DPR 2; `mobile-challenge.png`; `verified-challenge-default`     | `verified-challenge-default--390x844--dpr-2--verified--challenge-choice.{png,reference-normalized.png,overlay.png,diff.png,metadata.json}`       | P0       | `27.602%` unmasked pixel difference. The public route starts the setup gate rather than the approved challenge-choice composition.         | Added deterministic capture registration only; no redesign or unapproved image asset was introduced.                                            | All artifacts inspected. The state remains functionally usable, but is not visually accepted.                                             | Open — requires approved visual implementation; visual owner |
| W6-V03 | `/verified`, `calibration-guidance` | `390×844`, DPR 2; `mobile-calibration.png`; `verified-calibration-default` | `verified-calibration-default--390x844--dpr-2--verified--calibration-guidance.{png,reference-normalized.png,overlay.png,diff.png,metadata.json}` | P0       | `32.026%` unmasked difference. The approved calibrated-court visual and progress composition are absent.                                   | Preserved current semantic gate and truthful disabled/continue behavior; no screenshot-derived runtime asset was copied.                        | All artifacts inspected; existing gate controls remain keyboard-operable, but visual parity is absent.                                    | Open — requires approved visual implementation; visual owner |
| W6-V04 | `/verified`, `recording-capture`    | `390×844`, DPR 2; `mobile-record.png`; `verified-record-default`           | `verified-record-default--390x844--dpr-2--verified--recording-capture.{png,reference-normalized.png,overlay.png,diff.png,metadata.json}`         | P0       | `38.620%` unmasked difference. The approved recording composition/photo treatment is absent and requirements crowd the viewport.           | Kept the current explicit capture requirements and actual accessible controls; no false certification claim was added.                          | All artifacts inspected. The state exposes the required controls, but does not meet reference composition.                                | Open — requires approved visual implementation; visual owner |
| W6-V05 | `/verified`, `processing-pending`   | `390×844`, DPR 2; `mobile-processing.png`; `verified-processing-demo`      | `verified-processing-demo--390x844--dpr-2--verified--processing-pending.{png,reference-normalized.png,overlay.png,diff.png,metadata.json}`       | P0       | `19.506%` unmasked difference; the approved process timeline is absent.                                                                    | The current copy deliberately replaces the reference's closed-app notification promise with truthful foreground/manual-refresh guidance.        | All artifacts inspected. Truth is correct, but the composition remains visually unaccepted.                                               | Open — requires approved visual implementation; visual owner |
| W6-V06 | `/verified`, `ranked-report`        | `390×844`, DPR 2; `mobile-report.png`; `verified-ranked-policy-approved`   | `verified-ranked-policy-approved--390x844--dpr-2--verified--ranked-report.{png,reference-normalized.png,overlay.png,diff.png,metadata.json}`     | P0       | `18.576%` unmasked difference; the report lacks the approved compact mobile presentation.                                                  | Added an isolated, contract-parsed policy-approved ranked fixture solely for this rendering/capture. Demo and Free paths remain noncompetitive. | All artifacts inspected. The capture has persistent `Resultado validado — vale para ranking`; no demo result was used to imitate ranking. | Open — requires approved visual implementation; visual owner |

### Functional and accessibility evidence

- The production-built client has real-browser Free and Verified traces against the local demo Fastify API boundary. Both traces create via visible controls, upload real multipart bytes, observe pending, settle terminally, and query the live empty demo leaderboard. The browser suite records no console or page errors.
- The Free terminal contains no score, rank, percentile, verified, or leaderboard term/controls. The Verified demo terminal has persistent `Demo — não vale para ranking`, no rank/percentile/snapshot section, and the normal leaderboard remains empty.
- The ranked report above is isolated to `src/visual/ranked-policy-fixture.ts`, parsed through the public contract schemas. It is not used by the demo API path and does not calculate ranking policy in Web.
- Existing W0 structural coverage continues to verify semantic names, keyboard activation, focus landing, responsive bounds, reduced motion, and console/page cleanliness for the approved home view. W6 browser coverage verifies visible enabled/disabled upload controls, pending refresh, terminal truth, and no browser errors. These checks are evidence, not a claim of complete WCAG compliance.

### Strengths, risks, and evidence limits

- Strength: the W6 test command serves a production-built client and does not use MSW or Playwright routing for either required demo trace. The demo API check runtime keeps the real HTTP, C4/C5 storage, queue, worker, and demo-provider seam while supplying deterministic C10 probe/extraction facts.
- UX risk: the five verified mobile captures are visibly incomplete relative to their approved reference states; W6-V02 through W6-V06 are P0 and block acceptance.
- Accessibility risk: current controls are semantic and covered at the named interaction seams, but the long unstyled capture requirements create an avoidable mobile scanning burden (W6-V04). Full WCAG review remains outside screenshot evidence.
- Evidence limit: this host has no FFmpeg executable. The real host-codec demo start cannot run here, so the deterministic C10 check seam substitutes probe/extraction facts. The command remains secret-free; a codec-provisioned CI host must run the full host-media variant before final acceptance.
- Open P3 backlog: none. The P0 findings above are not waived and require remediation before independent acceptance.

### W6 remediation closeout — historical, 2026-09-02

The rows above are the first, intentionally failing comparison pass. The following second pass records implementation remediation, not independent acceptance. No visual source was copied into the runtime: the only image used by the five remediated states is the approved `public/assets/futsal-hero.png`; every non-home comparison remains unmasked. The changed-pixel percentage is evidence for inspection, not a waiver or a candidate-derived visual budget.

| Earlier ID | Route / exact state / viewport                       | Earlier severity and concrete fix                                                                                                                                                                                                                                                                            | Fresh after evidence                                                                                                                                                                                     | Five fidelity surfaces and residual disposition                                                                                                                                                                                                                                  | Final status / reviewer                                            |
| ---------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| W6-V02     | `/verified`, `challenge-choice`, `390×844` DPR 2     | **P0 fixed.** `VerifiedTracer` now starts at a mounted `ChallengeChoice`; `ChallengeChoice` supplies the 1/5 composition, wall-pass card, two non-interactive `Em breve` rows, Phosphor icons, and `Preparar desafio`. The owner cannot begin calibration/attempt work until that button moves to setup.     | `verified-challenge-default--390x844--dpr-2--verified--challenge-choice.{png,reference-normalized.png,overlay.png,diff.png,metadata.json}`; full unmasked ratio **23.596%**; all artifacts opened.       | Bebas/Arimo hierarchy, square rules, token colors, semantic copy/CTA, and mobile rhythm match the selected composition. The remaining P3 is the approved hero subject/crop versus the source photograph.                                                                         | Closed; P3 approved-asset variance pending Sol confirmation.       |
| W6-V03     | `/verified`, `calibration-guidance`, `390×844` DPR 2 | **P0 fixed.** `SetupProgress`, `CalibrationGuidance`, and `displayedPassedGates` render the actual five-gate rail, passed device marker, current correction rows, truthful blocking guidance, and approved-hero calibrated visual region. No client certification is invented.                               | `verified-calibration-default--390x844--dpr-2--verified--calibration-guidance.{png,reference-normalized.png,overlay.png,diff.png,metadata.json}`; full unmasked ratio **38.174%**; all artifacts opened. | The compact five-step rail, Bebas heading, rows, borders, and green/orange state colors are present and readable. The P3-only difference is the deliberately unavailable source court/measurement imagery; recreating it as CSS/SVG art or copying the screenshot is prohibited. | Closed; P3 approved-asset variance pending Sol confirmation.       |
| W6-V04     | `/verified`, `recording-capture`, `390×844` DPR 2    | **P0 fixed.** `ProductionCapture` keeps the real camera `<video>` visible when a stream exists, otherwise renders the approved hero fallback; it provides exact accessible recording/file controls and moves full requirements into semantic disclosure so they remain available without crowding the frame. | `verified-record-default--390x844--dpr-2--verified--recording-capture.{png,reference-normalized.png,overlay.png,diff.png,metadata.json}`; full unmasked ratio **34.494%**; all artifacts opened.         | Compact completed rail, display copy, preview hierarchy, green/outline controls, contrast, and disclosure affordance are visible at 390px. The P3 residual is photo subject/overlay variance only; live camera remains the truthful primary preview.                             | Closed; P3 approved-asset variance pending Sol confirmation.       |
| W6-V05     | `/verified`, `processing-pending`, `390×844` DPR 2   | **P0 fixed.** The pending owner now has the selected hierarchy, Phosphor progress timeline, `Atualizar agora`, and truthful foreground/manual-refresh copy. It deliberately removes the source's closed-app notification promise/control.                                                                    | `verified-processing-demo--390x844--dpr-2--verified--processing-pending.{png,reference-normalized.png,overlay.png,diff.png,metadata.json}`; full unmasked ratio **25.132%**; all artifacts opened.       | Bebas title, timeline rhythm, status colors, contrast, semantic ordered progress, and manual action remain usable. The P3 source-image-strip/notification difference is a required truth ruling, not a hidden or unimplemented control.                                          | Closed; P3 truth/approved-asset variance pending Sol confirmation. |
| W6-V06     | `/verified`, `ranked-report`, `390×844` DPR 2        | **P0 fixed.** `VerifiedReport` now uses a scorecard, metric definition list, ranked insight and persistent exact `Resultado validado — vale para ranking` only when the isolated parsed fixture is `ranked`; demo/experimental branches structurally omit rank fields and snapshots.                         | `verified-ranked-policy-approved--390x844--dpr-2--verified--ranked-report.{png,reference-normalized.png,overlay.png,diff.png,metadata.json}`; full unmasked ratio **16.812%**; all artifacts opened.     | The report has the reference hierarchy, condensed score/top-percent block, metric rules, copy, and token palette. The screenshot's metric values remain server fixture data; no ranking calculation occurs in Web. No material P3 remains beyond independent visual acceptance.  | Closed; independent Sol acceptance pending.                        |

Focused comparison was needed for V02 (challenge choice/CTA), V03 (passed gate and active correction), V04 (fallback versus live camera boundary), V05 (truthful refresh action), and V06 (ranked-only fields). Browser coverage confirms named controls, keyboard/focus behavior, enabled/disabled state, reduced motion, and clean console/page state; it does not claim complete WCAG conformance.

At this historical checkpoint, the production fixes were present, but the P0, P1, and P2 acceptance disposition for V02–V06 remained pending independent Sol review. Open P3 items were limited to the approved runtime hero or required truth replacement described above; neither was an implementation defect that could be corrected without violating the asset/truth constraints.

## Sol round-6 codec close boundary — historical, 2026-09-03

No visual surface or approved reference changed. The fixture codec lifecycle then reported child errors, non-zero exits, and cancellation only after the owned child closed; its deterministic coverage was **18/18** Node checks plus browser smoke **2/2**. It did not itself establish normal C10/Linux or independent Sol visual acceptance.

final result: pending independent Sol acceptance.

## W6 round-3 remediation — historical, 2026-09-02

This was the then-current implementation evidence from review base `168f16a`. It closed the reported implementation drift but did not self-issue acceptance: V02–V06 and every P0/P1/P2 disposition were pending independent Sol acceptance.

### Findings, fixes, and fresh comparison evidence

All evidence below is a fresh `390×844`, DPR 2, CSS-scale capture. For every verified state, I opened the normalized reference, candidate, 50% overlay, and complete pixel diff in `apps/web/coverage/playwright/visual-artifacts/`; they were nonblank, matched their named route/state, and showed no crop outside the viewport. W0 home masks and budgets were not changed.

| Earlier finding                                                                                               | Exact fix                                                                                                                                                                                                                                                           | Fresh evidence / result                                                                                                                                                                                                                                                                                                                  | Fidelity surfaces and disposition                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **W6-V02 / P0** — challenge paragraph crossed the hero photo instead of using the approved white-column wrap. | Mobile `.challenge-choice-hero p` now has `max-width: min(10rem, calc(60% - 3.6rem))`, keeping the three source-like lines fully left of the photo.                                                                                                                 | `verified-challenge-default--390x844--dpr-2--verified--challenge-choice.{reference-normalized.png,png,overlay.png,diff.png,metadata.json}` — **21.154% / 25.0%**. The candidate copy wraps as `Escolha um desafio validado` / `e prepare-se para superar` / `seu melhor.` and does not cross the image field.                            | Bebas/Arimo wrapping, white-column rhythm, warm-white/emerald token contrast, approved hero boundary, Portuguese copy, native disabled rows, and Phosphor affordances inspected. P0 closed in implementation; hero subject/crop remains the existing approved-asset P3 candidate for Sol.                                                                                      |
| **W6-V03 / P1** — vertically underscaled heading and excessive heading-to-visual gap.                         | The real calibration heading now uses source-relative vertical scaling and a measured offset; guidance margin is reduced so the existing approved hero crop follows at the selected rhythm. Static policy adds heading height/bottom and heading→visual gap checks. | `verified-calibration-default--390x844--dpr-2--verified--calibration-guidance.{reference-normalized.png,png,overlay.png,diff.png,metadata.json}` — **27.834% / 40.0%**. Raster inspection: reference and candidate heading ink both occupy y=`134–188`; DOM landmark is x=`27.30–282.19`, y=`132.37–200.83`, visual y=`232.75–450.34`.   | Bebas hierarchy/one-line wrapping, rail-to-heading-to-hero rhythm, warm-white/deep-emerald/orange state colors, approved hero crop, truthful blocker, correction copy, and semantic controls inspected. P1 closed in implementation; court subject variance is P3 for Sol.                                                                                                     |
| **W6-V04 / P1** — underscaled heading and excessive heading-to-preview gap.                                   | The real capture heading uses denser line-height plus source-relative vertical scaling; `ProductionCapture` margin keeps the actual/fallback preview at the reference-relative y position. Static policy adds heading height/bottom and heading→preview gap checks. | `verified-record-default--390x844--dpr-2--verified--recording-capture.{reference-normalized.png,png,overlay.png,diff.png,metadata.json}` — **31.061% / 36.0%**. Heading landmark x=`27.30–295.61`, y=`137.52–310.96`; preview y=`326.14–574.14`. Reference ink rows are `139–215` / `228–304`; candidate rows are `136–212` / `223–300`. | Bebas headline hierarchy, heading-to-preview rhythm, actual-camera/approved-fallback boundary, emerald control contrast, named keyboard controls, disclosure copy, and Phosphor upload affordance inspected. P1 closed in implementation. The 3–5px source-font raster offset is a non-actionable P3 pending Sol; the image subject difference remains approved-asset P3 only. |
| **W6-V05 / P2** — truthful pending hierarchy required fresh reinspection after layout remediation.            | No truth or runtime change: foreground/manual refresh remains, with no notification promise/control.                                                                                                                                                                | `verified-processing-demo--390x844--dpr-2--verified--processing-pending.{reference-normalized.png,png,overlay.png,diff.png,metadata.json}` — **24.992% / 30.0%**.                                                                                                                                                                        | Typography, timeline spacing, token contrast, truthful Portuguese copy, manual refresh affordance, and Phosphor status icons inspected. P2 closed in implementation; source notification/image-strip difference is required-truth/approved-asset P3 only.                                                                                                                      |
| **W6-V06 / P2** — ranked hierarchy required fresh reinspection after layout remediation.                      | No ranking behavior change: the isolated parsed ranked fixture retains exact persistent `Resultado validado — vale para ranking`; demo/experimental arms structurally omit ranking fields.                                                                          | `verified-ranked-policy-approved--390x844--dpr-2--verified--ranked-report.{reference-normalized.png,png,overlay.png,diff.png,metadata.json}` — **16.670% / 20.0%**.                                                                                                                                                                      | Bebas score/top-percent hierarchy, metric rules, token contrast, rank truth copy, isolated fixture data, and no non-Phosphor icon substitute inspected. P2 closed in implementation; independent Sol acceptance remains pending.                                                                                                                                               |

### Enforceable geometry and negative evidence

`referenceVisualGates` then stored independent V03/V04 headline height/bottom ranges plus sibling gaps before a candidate capture was evaluated. The then-current values were V03 height `64–74`, bottom `195–207`, heading→visual gap `28–38`; V04 height `166–180`, bottom `303–317`, heading→preview gap `10–22`. These DOM-box ranges are source-calibrated structural checks, not candidate-derived limits; they complement unmasked full-screen mismatch caps and required landmark bounds.

`visual-harness.test.ts` provides state-specific negative proofs: too-short V03/V04 headings, headings beyond their allowed bottom, and excessive heading→visual/preview gaps each throw. Existing over-budget, missing-landmark, and cropped-landmark negative tests remain intact. The capture spec passes `gate.referenceGaps` into this assertion, so browser captures enforce the policy rather than merely recording it.

### Browser and evidence limits

The approved-reference browser matrix reverified `scrollY=0`, no horizontal overflow, named semantics, Tab/Enter/Space, visible focus, disabled/enabled/loading states, reduced motion, and console/page-error handling at the exact viewport. This is observed evidence, not a full WCAG certification.

The normal C10 media acceptance was pending hosted CI after controller push because this machine had no FFmpeg. No `--serve-check` smoke result substituted for normal codec-backed acceptance, and the immutable Linux/x64 canonical pixel gate was hosted-only.

final result: pending independent Sol acceptance.

## W6 round-2 acceptance correction — historical, 2026-09-02

This was the then-current W6 authority. It superseded every earlier `Closed`, `fixed`, or `no P0/P1/P2 remains actionable` disposition in this document: the implementation and executable evidence were ready for review, but **V02–V06 and every P0/P1/P2 acceptance disposition remained pending independent Sol acceptance**. No reviewer result was self-issued there.

### Fresh complete-screen artifacts and inspection

The final `CI=1` W6 browser matrix regenerated and individually inspected the normalized reference, candidate, 50% overlay, and full diff for home plus all five verified states. All six state-specific comparisons remain unmasked; W0's home-only budget and mask were unchanged.

| State              | Fresh artifact stem                                                            | Full ratio / independently stored cap | Inspection result                                                                                                                                          |
| ------------------ | ------------------------------------------------------------------------------ | ------------------------------------: | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| W6-V01 home        | `home-default--390x844--dpr-2--root--ready`                                    |                W0 governed separately | The existing approved-runtime photo/crop variance remains a W0 concern, not a W6 budget change.                                                            |
| W6-V02 challenge   | `verified-challenge-default--390x844--dpr-2--verified--challenge-choice`       |                   **21.153% / 25.0%** | Back navigation, compact verified header, Bebas/Arimo heading, selected wall-pass card with `3 metros`, native disabled rows, and primary CTA are visible. |
| W6-V03 calibration | `verified-calibration-default--390x844--dpr-2--verified--calibration-guidance` |                   **29.236% / 40.0%** | Real five-gate rail, one-line compact heading, approved runtime hero crop, current correction rows, truthful blocker, and all four controls are visible.   |
| W6-V04 capture     | `verified-record-default--390x844--dpr-2--verified--recording-capture`         |                   **33.437% / 36.0%** | Completed rail, heading, actual-camera/approved-fallback preview boundary, record/file controls, and available requirements disclosure are visible.        |
| W6-V05 pending     | `verified-processing-demo--390x844--dpr-2--verified--processing-pending`       |                   **24.992% / 30.0%** | Foreground manual refresh and truthful no-notification-promise copy remain present.                                                                        |
| W6-V06 ranked      | `verified-ranked-policy-approved--390x844--dpr-2--verified--ranked-report`     |                   **16.670% / 20.0%** | Isolated ranked fixture retains exact `Resultado validado — vale para ranking`, scorecard, metrics, and ranked-only fields.                                |

The comparisons were inspected for the required fidelity surfaces: Bebas/Arimo typography and wrapping; mobile spacing/rhythm; warm-white/deep-emerald/border token contrast; approved asset boundary; Portuguese truth copy; semantic controls; and Phosphor affordances. Residual source-photo/court differences use only the already-approved runtime hero and remain reviewable P3 candidates; this record does not classify them as accepted.

### Reference-relative geometry and browser evidence

`referenceVisualGates` now contains static source-derived axis ranges for headline/card/CTA landmarks at `390×844`, in addition to the existing full-screen cap and required-landmark gate. The ranges are held in source before capture, rather than derived from the candidate: V02 heading/card/CTA; V03 heading/visual/action group; and V04 heading/preview/action group. `assertReferenceVisualLandmarkGeometry` rejects missing geometry, a wrong position, or a wrong size. Its unit negative mutations move the V02 heading beyond the source top range and shrink the V02 card beyond its right bound; both throw. W0 masks and budgets remain unchanged.

Fresh captured bounds at `scrollY=0` were: V02 heading `27.30–129.12 × 114.19–359.15`, selected card `27.30–362.70 × 462.39–581.22`, CTA `27.30–362.70 × 755.94–807.13`; V03 heading `27.30–282.19 × 125.17–173.05`, visual `0–390 × 236.75–454.34`, actions `27.30–362.69 × 741.08–816.25`; V04 heading `27.30–295.61 × 137.52–261.42`, preview `27.30–362.69 × 317.42–565.42`, actions `27.30–362.69 × 625.42–727.02`. The capture helper resets and verifies scroll before every screenshot; no named landmark overflows horizontally.

At `390×844`, the browser suite observed semantic names, Tab/Enter/Space, visible focus, disabled/enabled/loading behavior, reduced motion, responsive bounds, and console/page-error handling. The file input now has `tabIndex={-1}` and `aria-hidden="true"`; after Tab from the visible `Enviar vídeo existente` button, browser coverage proves focus reaches the visible `Requisitos da captura` button rather than the clipped native input.

### Round-2 RED/GREEN record and hosted codec limit

| Phase | Exact command / observed output                                                                                                                                                                                                                                                                   |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RED   | `rtk pnpm --filter @revelai/web exec vitest run src/visual/visual-harness.test.ts src/verified/production-capture.test.tsx --reporter=dot` initially failed on the absent `referenceGeometry` policy and because Tab reached the clipped `<input type=file>` instead of the requirements control. |
| RED   | `rtk env CI=1 pnpm --filter @revelai/web exec playwright test src/visual/approved-reference.visual.spec.ts --project=mobile-home --reporter=line` — **1 failed, 1 passed** while V02 heading/CTA, V03 action, and V04 heading/preview geometry were successively outside the source ranges.       |
| GREEN | `rtk pnpm --filter @revelai/web exec vitest run src/app.test.tsx src/visual/visual-harness.test.ts src/verified/production-capture.test.tsx --reporter=dot` — **3 files, 19 passed**.                                                                                                             |
| GREEN | `rtk env CI=1 pnpm --filter @revelai/web exec playwright test src/visual/approved-reference.visual.spec.ts --reporter=line` — **2 passed, 2 skipped**.                                                                                                                                            |
| GREEN | `rtk env CI=1 pnpm --filter @revelai/web run test:visual:structural -- --reporter=line` — **26 passed, 14 skipped**; `rtk env CI=1 pnpm --filter @revelai/web run test:visual:darwin -- --reporter=line` — **30 passed, 10 skipped**.                                                             |
| GREEN | `rtk pnpm check` — exit **0**: format, **7/7** lint, **12/12** typecheck, **12/12** test tasks, **7/7** build.                                                                                                                                                                                    |

The local host still has no FFmpeg. Normal codec-backed `test:demo:e2e` acceptance therefore remains an honest local environment limit, and the codec-provisioned hosted CI run is **pending hosted green** after controller push; no check-fact smoke is substituted for that acceptance.

final result: pending independent Sol acceptance.

## W6 round-1 reviewer remediation — 2026-09-02

This was the then-authoritative after-record for reviewer findings V02–V06. It superseded the older W6 closeout ratios and claims where they conflicted. This historical record is itself superseded by Final W6 acceptance. Fresh artifacts were emitted by the final `rtk pnpm check` structural run and then opened individually: reference-normalized PNG, capture PNG, 50% overlay, and complete pixel diff for home plus all five `/verified` states. No state-specific image region is masked.

### Enforceable reference gates and bounds

`src/visual/visual-harness.ts` now keeps a static, per-state reference policy rather than accepting every non-home comparison. The values are an independently stored W6 acceptance calibration for the selected references: they account only for the documented approved hero/crop or required-truth deltas, while preserving the entire candidate image. They are not written from a candidate capture. Required semantic landmarks are a second, orthogonal gate, so a visually small or high-level mismatch cannot conceal absent or cropped UI.

| State                | Independent maximum mismatch | Required landmark evidence                                          | Fresh mismatch | Result |
| -------------------- | ---------------------------: | ------------------------------------------------------------------- | -------------: | ------ |
| challenge choice     |                        25.0% | header, heading, selected card, prepare CTA                         |        21.188% | pass   |
| calibration guidance |                        40.0% | header, rail, heading, truth panel, confirm, continue, back, cancel |        31.839% | pass   |
| recording capture    |                        36.0% | header, compact rail, heading, preview, record, file-select         |        33.405% | pass   |
| processing pending   |                        30.0% | header, heading, timeline, refresh, reset                           |        25.132% | pass   |
| ranked report        |                        20.0% | header, ranking truth, heading, scorecard, metrics                  |        16.812% | pass   |

`assertReferenceVisualMismatch` makes a ratio above the listed limit fail after artifacts are written; `assertReferenceVisualLandmarks` rejects a missing ID, a zero-size control, or any landmark outside `390×844`. The W0 home budget/mask and its immutable limits were not changed. `visual-harness.test.ts` includes negative mutation proofs: a missing `setup-confirm`, an intentionally cropped `setup-cancel`, and an over-budget calibration mismatch all throw. The focused unit command passed **5/5**; these negative assertions are executable, rather than a metadata-only claim.

The capture helper resets scroll to `(0,0)` and polls `scrollY === 0` before every screenshot. Heading focus uses `focus({ preventScroll: true })`. The final metadata records these viewport measurements: V03 header `0–58.39`, rail `66.39–101.19`, heading `115.58–211.33`, truth `546.02–573.84`, confirm/continue `581.03–613.83`, and back/cancel `618.63–651.42`; V04 header `0–58.39`, rail `66.39–123.13`, heading `137.52–227.61`, preview `293.63–469.63`, record `505.63–546.42`, file selection `552.02–592.81`, and submit `642.59–686.59`. Every named control is therefore fully inside the approved viewport at capture time.

### Historical reviewer findings — implementation evidence

| Earlier finding                                                        | Exact production fix                                                                                                                                                                                                                                                                                          | After artifact / ratio                                                                                                                                                  | Required fidelity surfaces and focused comparison                                                                                                                                                                                                                                            | Disposition                                                                              |
| ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| **V02 / P0:** missing complete challenge composition and `3 metros`    | `ChallengeChoice` retains the real owner boundary, adds Phosphor `ArrowsLeftRight` plus truthful `3 metros`, and converts both `Em breve` rows from `aria-disabled` articles to native disabled buttons with one accessible explanation. The heading/card/CTA landmarks enforce composition.                  | `verified-challenge-default--390x844--dpr-2--verified--challenge-choice.{reference-normalized.png,png,overlay.png,diff.png,metadata.json}` — **21.188% / 25.0%**.       | Bebas/Arimo wrapping, editorial square rules, warm-white/emerald contrast, spacing, real wall-pass card, native disabled semantics, and Phosphor-only affordances. Focused check: Tab reaches `Preparar desafio`, outline is visible, and disabled `Em breve` remains disabled.              | Closed. Only approved hero subject/crop variance is P3.                                  |
| **V03 / P0:** cropped calibration controls / uncontrolled focus scroll | Mobile calibration CSS compacts the real five-gate rail, heading, actual correction rows, truthful blocking panel, and four existing controls. Capture resets scroll and heading focus no longer scrolls the page. No client certification is fabricated.                                                     | `verified-calibration-default--390x844--dpr-2--verified--calibration-guidance.{reference-normalized.png,png,overlay.png,diff.png,metadata.json}` — **31.839% / 40.0%**. | Rail, passed/current state, Bebas heading, approved existing hero crop, truthful disabled `Continuar`, square buttons, compact body rhythm. Focused comparison checks all V03 landmark bounds at `scrollY=0`; `Confirmar etapa` receives a visible keyboard focus ring.                      | Closed. The unavailable court-art subject is P3 approved-asset variance only.            |
| **V04 / P0/P1:** capture composition and invisible file focus          | `ProductionCapture` retains actual camera/video when available and approved hero fallback only without it. The clipped file input is now invoked by a named native button with Phosphor `UploadSimple`, global visible focus styling, and keyboard Space support. Requirements remain in semantic disclosure. | `verified-record-default--390x844--dpr-2--verified--recording-capture.{reference-normalized.png,png,overlay.png,diff.png,metadata.json}` — **33.405% / 36.0%**.         | Completed rail, compact Bebas hierarchy, preview boundary, green/outline controls, token contrast, and requirements disclosure. Focused browser proof Tabs to the file button, observes its focus ring, then Space opens the real file chooser; disabled/enabled submit states are asserted. | Closed. Live/fallback photo subject is the sole P3 variance.                             |
| **V05 / P0:** pending hierarchy could falsely promise notification     | Pending state keeps the semantic ordered timeline, manual foreground refresh and reset. It explicitly says processing continues on the server and makes no closed-app notification promise/control.                                                                                                           | `verified-processing-demo--390x844--dpr-2--verified--processing-pending.{reference-normalized.png,png,overlay.png,diff.png,metadata.json}` — **25.132% / 30.0%**.       | Bebas hierarchy, timeline rhythm, green status contrast, Portuguese truthful copy, manual actions. Focused browser comparison verifies enabled refresh, disabled/`aria-busy` loading refresh, then enabled refresh after completion.                                                         | Closed. Source notification/image-strip conflict is required truth replacement, P3 only. |
| **V06 / P0:** ranked arm needed truth and structural rank isolation    | `VerifiedReport` exposes scorecard/metrics only for the isolated schema-parsed ranked fixture and persistently renders exact `Resultado validado — vale para ranking`; demo/experimental branches structurally lack rank, percentile, top-percent, and snapshot fields.                                       | `verified-ranked-policy-approved--390x844--dpr-2--verified--ranked-report.{reference-normalized.png,png,overlay.png,diff.png,metadata.json}` — **16.812% / 20.0%**.     | Ranking truth, condensed score/top-percent hierarchy, metric rules, Arimo values, Bebas labels, token contrast, and Ranked-only `Ver Ranking atual`. Focused comparison checks the exact truth text and public Ranking link under the isolated ranked fixture.                               | Closed. No actionable P3 beyond independent Sol review.                                  |

### Browser accessibility/use evidence actually observed

`approved-reference.visual.spec.ts` runs at exactly `390×844`, DPR 2. It asserts semantic names, Tab then Enter/Space action, visible focus, and the enabled/disabled/loading states described above across challenge, calibration, capture, processing, and ranked report. A second mobile audit observes the public leaderboard loading button disabled, expected safe failure alert, keyboard refresh, empty state, reduced motion, and no horizontal overflow. The happy-state matrix records no console errors and no `pageerror`; the failure audit filters only the browser's expected HTTP 503 resource message and rejects every other console/page error. This is browser evidence for those specified interaction seams, not a claim of complete WCAG conformance.

### Codec-backed C10 evidence and hosted limit

The required normal demo server no longer appends `--serve-check`. It generates actual MP4 fixtures before the API starts: Free portrait `720×1280`, `3 s`, `24 fps`; Verified landscape `1280×720`, `64 s`, `24 fps`. `demo-media-fixtures.mjs` invokes FFmpeg and then reads FFprobe JSON to assert those dimensions, durations, and frame rate before either browser trace uploads the media. The `demo-media-fixtures.test.mjs` negative-safe command injection test verifies two FFmpeg and two FFprobe invocations (**1/1 pass**). `--serve-check` now has its own `playwright.demo.smoke.config.ts` and is only the separate test-only smoke (`2/2 pass`), never the required real-media command.

The local host has no `ffmpeg`; `rtk pnpm --filter @revelai/web run test:demo:e2e` therefore fails honestly before the server starts with `Error: spawn ffmpeg ENOENT` and the shown FFmpeg arguments. This is an explicit environment limit, not a downgraded acceptance. `.github/workflows/ci.yml` installs FFmpeg with `apt-get` before the unchanged normal `test:demo:e2e` command, so hosted CI is the codec-provisioned real-media acceptance environment. Its result is still pending a hosted run.

### Round-1 RED/GREEN command record

| Phase                      | Exact command / result                                                                                                                                                                                                             |
| -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RED                        | `rtk pnpm --filter @revelai/web exec vitest run src/visual/visual-harness.test.ts --reporter=dot` — **1 failed, 4 passed (5)** before `getReferenceVisualGate` and enforcement existed.                                            |
| RED                        | `rtk pnpm --filter @revelai/web exec vitest run src/verified/tracer.test.tsx -t 'states the three-metre' --reporter=dot` — **1 failed, 42 skipped (43)** before the truthful 3m row/native disabled buttons.                       |
| RED                        | `rtk pnpm --filter @revelai/web exec vitest run src/verified/production-capture.test.tsx -t 'uses a focusable button' --reporter=dot` — **1 failed, 10 skipped (11)** before keyboard file selection.                              |
| RED                        | `rtk node --test apps/web/scripts/demo-media-fixtures.test.mjs` — **1 failed** before the codec fixture module existed.                                                                                                            |
| RED                        | `rtk pnpm check` — **1 failed suite, 286 passed tests** after the first Node test filename was collected by Vitest. `vitest.config.ts` now excludes `scripts/**/*.test.mjs` and the Web package invokes that Node test explicitly. |
| GREEN                      | `rtk pnpm check` — exit **0**: format; **7/7** lint; **12/12** typecheck; **12/12** test tasks; **7/7** build. Web: **12 Node checks**, **27 Vitest files / 286 tests**, structural browser **26 passed / 14 skipped**.            |
| GREEN                      | `rtk pnpm --filter @revelai/web exec playwright test src/visual/approved-reference.visual.spec.ts --project mobile-home --config playwright.config.ts --reporter=line` — **2 passed**.                                             |
| GREEN                      | `rtk pnpm --filter @revelai/web run test:production-router` — **23 passed**.                                                                                                                                                       |
| GREEN                      | `rtk pnpm --filter @revelai/web run test:demo:e2e:smoke` — **2 passed**.                                                                                                                                                           |
| GREEN                      | `rtk env CI=1 pnpm --filter @revelai/web run test:visual:structural -- --reporter=line` — **26 passed / 14 skipped**.                                                                                                              |
| GREEN                      | `rtk env CI=1 pnpm --filter @revelai/web run test:visual:darwin -- --reporter=line` — **30 passed / 10 skipped**.                                                                                                                  |
| Expected hosted-only limit | `rtk env CI=1 pnpm --filter @revelai/web run test:visual:canonical -- --reporter=line` — exit **1** on Darwin arm64: `Canonical visual pixels require linux/x64.` The immutable Linux/x64 CI gate remains unchanged.               |
| GREEN                      | `rtk git diff --check` — exit **0** before functional commit.                                                                                                                                                                      |

At this historical checkpoint, the executable visual, bounds, media, and browser evidence supported the implemented fixes, but all reviewer P0/P1/P2 acceptance dispositions remained pending independent Sol review. Hosted codec execution and the unchanged canonical Linux/x64 pixel gate also remained pending. Deferred P3 component/style seams and historical report-table cleanup remained intentionally outside that round.

final result: pending independent Sol acceptance.

## W6 round-2 disposition — historical and superseded

The earlier round-1 section was historical evidence only. The round-2 record above was the implementation record for the then-final fresh artifacts; **V02–V06 and all P0/P1/P2 acceptance dispositions were pending independent Sol acceptance**. Codec-backed normal-runtime acceptance was also **pending hosted green** after controller push. No earlier `Closed` wording was a current acceptance result.

final result: pending independent Sol acceptance.

## W6 round-3 disposition — historical and superseded

The round-3 remediation evidence above superseded the historical round-2 geometry, ratios, and implementation disposition. V02–V06 had no actionable P0/P1/P2 implementation finding, but every acceptance disposition remained pending independent Sol acceptance; hosted codec and canonical Linux/x64 evidence remained controller work.

final result: pending independent Sol acceptance.

## CI round 5 runtime-boundary note — 2026-09-02

Functional commit `ef2ff28` changes no visual surface, approved asset, visual gate, W0 budget, or reference artifact; no visual recapture is claimed. The normal demo E2E child strips only Playwright's private `REVELAI_DEMO_E2E` marker before strict API validation. Codec-provisioned Ubuntu browser evidence reaches the real V05 pending composition with truthful timeline and manual refresh after upload. Its nonterminal Docker run is Rosetta amd64-emulation throughput, not a visual result or native hosted acceptance. Existing V02–V06 inspection remains applicable; native Linux/x64 codec completion and independent Sol review remain required.

final result: pending independent Sol acceptance.

## CI P1 acceptance-harness correction — 2026-09-02

Functional commits `e6aa4fc` and `e858e51` alter no visual state or approved artifact. The earlier browser claim is narrowed to observed evidence: the Verified trace now requires upload progress, then the separate pending main named `Processando tentativa` and enabled visible `Atualizar agora`, before its terminal report assertion. A progressbar alone is not pending evidence. The real wrapper is also covered against a foreign health-200 owner, a hanging foreign socket, and a wrapper setup error carrying an absolute local path; all three fail safely without accepting foreign readiness or disclosing that path. Existing V02–V06 visual inspection remains applicable. Native Linux/x64 codec terminal evidence and independent Sol review are still required.

final result: pending independent Sol acceptance.

## CI child lifecycle correction — 2026-09-02

Functional commit `e99733f` changes no visual state or accepted artifact. The verified browser trace has one 120-second post-upload budget shared by progress, pending, and terminal observations; it no longer permits independent 120-second pending and terminal waits. The browser harness consumes only its child’s exact readiness token and never emits child stdout/stderr. Regressions prove that a pre-setup child stack, local path, and sentinel secret do not reach wrapper output, and that successful and SIGTERM-resistant owned children release both 4174 and 4175 before their wrapper completes. Existing V02–V06 visual inspection remains applicable. Native Linux/x64 codec terminal evidence and independent Sol review are still required.

final result: pending independent Sol acceptance.

## CI lifecycle edge-case correction — 2026-09-03

Functional commit `27df4ce555eca77df74c86c95ab2035d6be55dd2` changes no captured visual surface, asset, W0 budget, or reference gate, so no fresh visual artifact is claimed. The wrapper now keeps its owned child error listener through TERM and SIGKILL until that exact child closes, and concurrent shutdown callers share one cleanup promise. Wrapper regressions cover a normal child and a SIGTERM-resistant owned child; each run proves ports 4174 and 4175 can be rebound only after wrapper completion.

The real Verified browser trace now gives the enabled `Atualizar agora` expectation the same decreasing 120-second post-upload remainder already used for progress, pending, and terminal report. This corrects the prior implicit 5-second default and preserves the 150-second outer teardown/reporting backstop. Focused Node lifecycle tests pass **2/2**, combined wrapper tests **8/8**, and the smoke browser run **2/2**; the normal codec-backed terminal and Linux/x64 canonical visual evidence remain hosted-controller work.

final result: pending independent Sol acceptance.

## Sol round-4 signal/exit correction — 2026-09-03

Functional commit `259fd4e` changes no visual artifact or accepted visual policy. The wrapper installs persistent SIGINT/SIGTERM handlers before startup awaits, so a signal before API readiness and a repeated SIGTERM during the owned child's TERM grace share the same cleanup and leave 4174/4175 rebindable after wrapper close. The child lifecycle also recognizes a non-null `signalCode` while awaiting `close`, so an already TERM-exited child never receives an unnecessary SIGKILL. The lifecycle/wrapper suite passes **11/11** and browser smoke **2/2**. Hosted normal-codec and canonical evidence remain pending.

final result: pending independent Sol acceptance.

## Sol round-5 fixture lifecycle correction — 2026-09-03

Functional commit `c2816a6` changes no visual artifact or visual gate. Normal fixture generation is now an owned cancellable startup resource: a signal closes admission, waits for every already-admitted codec to close, prevents API/Web continuation, and removes fixture media. The controlled integration records both codec closes before wrapper close, no API-start marker, absent temporary media directory, and rebindable 4174/4175; the Node suite passes **15/15** and browser smoke **2/2**. Normal hosted codec and canonical evidence remain pending.

final result: pending independent Sol acceptance.

## Final W6 acceptance — 2026-09-03

**Status: DONE. Result: passed by independent Sol review.** Hosted workflow [33712809662](https://github.com/ToledoVitor/revelAI/actions/runs/33712809662) at `12318cb43a9669f67caa9eb462cbfd1f48a69f3d` passed every gate with exit 0: production-router **23/23**; real-FFmpeg/C10 demo E2E Node **18/18** plus browser **2/2**; canonical Linux/x64 visual **30 passed, 10 skipped**; focused API **23/23**; and operability **18/18**. The complete quality job took **12m14s** (with `pnpm check` at about **7m31s**); the complete clean-executable/operability job took **2m54s**.

The approved truth rulings remain part of the accepted UI: V05 retains foreground/manual refresh and no closed-app notification promise; V06 retains the isolated ranked fixture and exact `Resultado validado — vale para ranking`, while Free/demo/experimental paths structurally omit rank fields. The documented approved-runtime hero/crop variance remains accepted evidence; no reference screenshot, invented asset, or false competitive claim was introduced. This is the sole current W6 authority in this document; every earlier checkpoint is historical and superseded.

final result: passed
