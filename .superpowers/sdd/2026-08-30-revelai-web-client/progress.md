# SDD ledger — plan: docs/superpowers/plans/2026-08-30-revelai-web-client.md

Spec authority: `docs/specs/2026-08-30-revelai-mvp.md`.

## Preflight dependency/interface scan

| Tasks      | Shared file/interface                      | Producer → consumer                                        | Finding                                                                        |
| ---------- | ------------------------------------------ | ---------------------------------------------------------- | ------------------------------------------------------------------------------ |
| W0 / W1    | router, providers, home/history navigation | W0 shell → W1 parsed API and history                       | Clean: W0 exposes unavailable target; W1 activates history only.               |
| W0 / W2    | router and home controls                   | W0 unavailable shell → W2 dev/test review setup            | Clean: production verified remains unavailable.                                |
| W0 / W4    | home verified control and public router    | W0 unavailable control → W4 sole `/verified` owner         | Clean: activation ownership explicit.                                          |
| W0 / W5    | home Free control and public router        | W0 unavailable control → W5 sole Free activation           | Clean: activation ownership explicit.                                          |
| W1 / W3    | multipart transport fixtures               | W1 exact API client → W3 review upload UI                  | Clean: W3 consumes transport, does not create production attempt.              |
| W1 / W4    | API client, identity, attempt outcomes     | W1 parsed client → W4 verified orchestration               | Clean: score/rank remain server-owned.                                         |
| W1 / W5    | API client, identity, history              | W1 parsed client/history → W5 Free orchestration/delete    | Clean: device-local identity wording preserved.                                |
| W2 / W3    | review route switch and fake ports         | W2 shared `reviewRoutesEnabled` → W3 capture route         | Clean: same exact switch required; production omits both routes.               |
| W2 / W4    | setup state/components                     | W2 review-only setup → W4 production composition           | Clean: W4 alone performs production mutations.                                 |
| W3 / W4    | capture/upload state/components            | W3 review-only capture → W4 production composition         | Clean: W4 owns production attach and pending state.                            |
| W4 / W5    | router, outcome boundaries, history        | W4 verified-only terminal renderer → W5 Free-only renderer | Clean: tagged outcomes prevent overlap.                                        |
| W0–W5 / W6 | routes, fixtures, visual harness           | functional nodes → browser/visual acceptance               | Clean: W6 adds QA only, no feature logic.                                      |
| W0         | runtime/home/style/test harness            | own tests ↔ own code                                      | Clean: two viewports, accessibility, asset gate, deterministic metadata align. |
| W1         | client/history                             | malformed-schema tests ↔ parsed transport                 | Clean: exact C8 contracts already accepted.                                    |
| W2         | setup gates                                | production-isolation tests ↔ dev/test-only route          | Clean: no production mutation before W4.                                       |
| W3         | capture/upload                             | timer/media tests ↔ browser adapter                       | Clean: exact durations/MIME ordering internally consistent.                    |
| W4         | verified tracer                            | route-isolation/outcome tests ↔ sole production owner     | Clean: GW follows W4 and does not block producer.                              |
| W5         | Free tracer                                | structural absence tests ↔ Free-only UI                   | Clean: no competitive vocabulary or client-derived tips.                       |
| W6         | acceptance                                 | reference captures ↔ real demo flows                      | Clean: visual truth cannot override product truth.                             |

No plan/spec conflict found. User explicitly authorized direct commits and pushes to `main`; task review still gates each push.

Task W0: initial review at `6edadcc` — Spec FAIL, Quality Needs fixes. Functional scope/API truth passed. Open: 2 Critical (mobile target off-viewport; executable reference capture/diff gate absent), 4 Important (visual suite outside standard CI, incomplete behavior RED evidence, partial keyboard/focus proof, unbundled display font), 1 Minor (narrow zero-API observation). Fix round 1/5 resumed with original Terra xhigh; no push.

Task W0: fix round 1/5 at `3cf45d9` — 6 findings addressed, 2 Important open (historical TDD overclaim; visual mask/tolerance hides major UI). Controller visual QA also found desktop hero boundary at 35% versus approved ~46%; mobile now fits 390×844 with no console noise. Fix round 2/5 resumed with original Terra xhigh; no push.

Task W0: fix round 2/5 at `fd8af76` — Sol scoped re-review APPROVED all 3 findings; design QA passed and controller browser confirmed x=668 desktop split, exact 390×844 mobile height, and zero browser console issues. Controller fresh CI-mode web gate then exposed Node `NO_COLOR`/`FORCE_COLOR` warnings in Playwright/webServer despite 15 pass/3 skip. Fix round 3/5 resumed with original Terra xhigh to remove cause portably; no push.

Task W0: fix round 3/5 at `be3c8d0` — original warning finding ADDRESSED and controller exact CI-mode gate is pristine (13 Vitest + 15 Playwright, 3 skips). Sol found new Important: runner integration test launches Chromium during Vitest before `pretest:visual` installs it, so clean local cache can fail before installer runs. Fix round 4/5 assigned to fresh Terra xhigh per SDD escalation; no push.

Task W0: fix round 4/5 at `77bae8f` — clean-cache ordering finding ADDRESSED with idempotent provisioning before Vitest and direct visual runs. Sol found new Important: installer wrapper did not forward or preserve terminal signals and could orphan a download. Fix round 5/5 resumed with the fresh Terra xhigh implementer; no push.

Task W0: fix round 5/5 at `33a4c4c` — signal lifecycle finding ADDRESSED; Sol final scoped review CLEAN with 0 Critical/Important open. Clean-cache public command, exact CI web gate, full visual suite, and root checks recorded green. Awaiting controller gates, push, and hosted CI.

Task W0: pushed `33a4c4c`; hosted CI `33584092588` operability passed but Ubuntu quality exposed an OS-dependent body-font fallback: desktop/mobile visual budgets failed and the mobile CTA bottom reached 852.45px. Systematic evidence showed only Bebas Neue was bundled while the Inter token resolved to different host fonts; additionally `:root` consumed the token before `.app-shell` defined it.

Task W0: hosted-CI portability fix at `2ecb9c0` — bundled OFL Arimo 400/700 as the truthful shared body token, consumed it on `.app-shell`, and added a real Chromium FontFaceSet regression. Existing masks/budgets stayed unchanged; 90% ink floors were recalibrated to deterministic capture and mutation rejection remains green. Sol review CLEAN with 0 Critical/Important. Awaiting controller gates, push, and hosted Linux confirmation.

Task W0: pushed `2ecb9c0`; hosted CI `33586142017` confirmed font readiness and viewport bounds but still failed both normal pixel gates. Pinned Linux reproduction proved full-image and focused-ink ratios were already within unchanged budgets; only Darwin-derived absolute ink coverage floors differed under Linux antialiasing.

Task W0: canonical-renderer architecture `48c15df..255b0fe` — portable structural checks remain in `pnpm check`; required reference/image/ink/static-oracle/mutation checks run in an immutable Playwright Linux/amd64 image. Added independent renderer-specific nonzero floors, strict mode/identity validation, digest/platform/network isolation, offline action-pnpm mount, clean-cache lifecycle separation, and cross-platform Node + `npm_execpath` child invocation. Sol review rounds addressed Windows scripts, canonical identity/hermeticity, direct structural provisioning, and `.cmd` spawning; final Critical/Important review CLEAN, minor wording corrected. Awaiting controller gates, push, and hosted CI.

Task W0: CI recovery complete at `08e7df8` — hosted run `33595017365` passed both jobs (operability 2m37s; quality 6m40s; canonical Linux visual gate 1m14s). Final failures were a cold-run smoke timeout and pnpm 11 path-sensitive dependency verification: the Docker gate relocated both the absolute store path and workspace roots, triggering its default implicit install under `--network none`. Commits `fcf6f1a`, `09abefc`, and `08e7df8` isolate the smoke server, preserve host workspace/store paths inside the pinned container, force offline fail-fast verification, and regression-test both Docker invocations independently. Terra xhigh implementation and Sol xhigh final review CLEAN; controller focused test, root `pnpm check`, hosted visual gate, OpenAPI, API smoke, operability, local/remote equality, and clean worktree all passed. Pushed directly to `main`.

Task W0: complete (commits `eef12bb..08e7df8`, review clean, hosted CI passed).

Ruling: SDD `task-brief` accepts only numeric `Task N` headings while this accepted plan uses `W0`–`W6` headings — create exact manual `task-WN-brief.md` extracts instead of renaming plan identifiers — cost if wrong: manual briefs can drift if plan changes, so each dispatch must compare its brief against current plan text.

Task W1: started from `08e7df8`; brief manually matched against current W1 plan requirements; Terra xhigh implementation pending.

Task W1: initial implementation `29d0a34..1e18332`; Sol xhigh review Spec ❌ / Quality Needs fixes. Open Important: malformed RouteError plus code↔status mismatch lacks negative proof; delete loses focus/live progress and fixed aria-label masks pending state; mobile menu's DOM order lets real Tab skip navigation after opening.

Task W1: minor (deferred): upload test consumes accepted outcome but not shared C2 accepted request descriptor, so request-fixture drift is not directly proven; final whole-branch review must triage.

Task W1: fix round 1/5 (`1e18332..9dd33eb`) — all 3 Important findings addressed; Sol xhigh scoped re-review found 0 new Critical/Important. Deferred request-descriptor parity remains outside this fix loop.

Task W1: complete (commits `08e7df8..9dd33eb`, task review clean). Awaiting controller gates, push, and hosted CI.

Task W1: pushed `9dd33eb`; hosted CI `33600218015` exposed one Linux cold-start timeout in the real-Chromium Playwright runner regression (`playwright-runner.test.ts:50`, 5011 ms against Vitest's 5000 ms default). Other 97 web tests passed and operability passed. W2 paused; fix round 2/5 resumed with original Terra xhigh, no follow-up push yet.

Task W1: fix round 2/5 (`9dd33eb..c43c988`) — scoped 15 s finite budget only for the real-Chromium cold-start integration; runner, browser smoke, exit code, `1 passed`, and `NO_COLOR` assertions unchanged. Sol xhigh review CLEAN; controller web 98, structural 18/8 skips, root check, and diff check passed. Awaiting hosted Linux confirmation.

Task W1: hosted CI recovery complete at `c43c988` — run `33600876484` passed quality in 8m14s (root check, canonical Linux visual gate, OpenAPI, focused operability) and clean executable/operability probes in 3m07s. Local/remote main equal and W1 complete.

Task W2: started from `c43c988`; manual brief matched against current W2 plan and MVP spec; fresh Terra xhigh implementation pending.

Task W2: initial implementation `7975c59..225b627`; Sol xhigh review Spec FAIL / Quality Needs fixes. Open: 7 Important (missing selectable challenge list; missing visible 4 s pre-roll/60 s active guidance; contradictory post-pass gate status; second injectable router switch instead of sole environment switch; stale-dist/no-chunk false-positive production proof; no full/repeated zero-mutation proof; missing route/gate/completion focus and terminal action) and 1 Minor (production review URLs need route-safe setup-guidance-unavailable copy). Fix round 1/5 resumed with original Terra xhigh; no push.

Task W2: fix round 1/5 (`225b627..21ae086`) — 6 original Important plus original Minor addressed; sole-switch production graph absence is green, but the required first-layer production-valued navigation/mutation integration proof remains open. Sol found one new Important (artifact builder depends on prior contracts dist, so direct clean-checkout command is not self-contained) and two new Minor (subprocess signal/error lifecycle; unstyled selection/completion controls). Fix round 2/5 resumed with original Terra xhigh; no push.

Task W2: fix round 2/5 (`21ae086..83e7e65`) — production-transformed router harness, dependency-aware clean artifact build, subprocess lifecycle, and tokenized controls addressed the open findings. Sol found one new Important: partial clean-output staging or process termination could leave workspace dependency outputs moved. Fix round 3/5 resumed with original Terra xhigh; no push.

Task W2: fix round 3/5 (`83e7e65..2db7412`) — unique OS-temp staging, rollback state retention, exact restoration, and signal-aware child settlement addressed partial staging and cancellation paths. Sol's real-process analysis found one remaining Important: `.once` signal handlers allowed a repeated termination signal to restore Node's default exit before recovery completed. Fix round 4/5 resumed with original Terra xhigh; no push.

Task W2: fix round 4/5 (`2db7412..0de03ee`) — persistent signal handlers consume repeated and mixed terminal signals through cleanup while forwarding only the first. Real subprocess proof holds restoration, sends three SIGTERMs, verifies the process remains alive, restores exact markers, removes listeners, and exits normally. Sol final scoped review: Spec PASS, Quality Approved, CLEAN with no Critical, Important, or Minor findings.

Task W2: controller verification passed: clean dependency-output recovery 6/6, production-transformed router/setup 16/16, served production isolation 4/4, development lazy-import smoke 1/1, root `pnpm check`, and diff checks. Awaiting push and hosted CI confirmation.

Task W2: hosted CI recovery complete at `1dbe536` — run `33616043402` passed quality in 8m21s (`pnpm check`, production-router 4/4, canonical Linux visuals, OpenAPI, focused operability) and clean executable/operability probes in 2m29s. Earlier runs `33611887462` and `33613860849` produced durable regressions for the scoped production-graph budget and React/BrowserRouter commit synchronization. Sol final reviews CLEAN; controller stress, production browser, root gates, local/remote equality, and clean worktree passed.

Task W2: complete (commits `c43c988..1dbe536`, review clean, hosted CI passed).

Task W2: hosted CI regression on `33611887462` — Ubuntu `pnpm check` failed only because the real production Vite graph assertion in `apps/web/src/app.test.tsx` reached Vitest's implicit 5 s limit at 5,304 ms; its sibling production-router harness passed in 9,357 ms. Diagnosis found finite contention, not a graph assertion failure, clean-dist residue, or a hang: isolated CI-mode graph runs were 1.56–1.71 s; 24 guarded CPU workers reproduced the same timeout even for the graph test alone; the same load completed in 6.65/6.91 s with a temporary finite 15 s budget; clean-output recovery followed by immediate graph build stayed green. Linux/amd64 pinned-container emulation was unsuitable as a tight local loop. Functional candidate `98dd490` adds only named, scoped `productionGraphIntegrationTimeoutMs = 15_000` to that existing graph `it`; graph/module/marker assertions, production environment handling, and global Vitest configuration remain unchanged. The same guarded stress loop is GREEN twice (6.56/6.23 s); focused W2, web lint/typecheck/build/test, root `pnpm check`, and diff checks passed. Sol review pending; no push.

Task W2: hosted CI run `33613860849` — prior app graph budget passed in 8,802 ms, but `production-router-harness.test.ts` (9,180 ms) intermittently left only in-app setup at Home while direct setup/capture and in-app capture passed. Root cause: the harness relied on one `setTimeout(0)` after React 19 `createRoot().render()` and another after `popstate`; BrowserRouter registers `history.listen` in `useLayoutEffect`, browser-history ignores popstate before that listener exists, and the normal listener update uses `startTransition`. Controller direct selected-test concurrency reproduced the exact stale Home in 1/4 and 1/8 fresh processes, ruling out order/leak; every run settled, so no hang. Functional candidate `1678b76` uses explicit-container, 1 s finite `waitFor` conditions for Home readiness before `popstate`, the existing unavailable isolation boundary after it, direct boundaries, and final unmount emptiness; no app/router/W3 behavior changed. GREEN: 32/32 direct-binary concurrent in-app setup cases, focused W2 16/16, web lint/typecheck/build/test (11 Node / 114 Vitest / 20 structural + 8 expected skips), production artifact 4/4, root `pnpm check`, and diff checks. No residual stress/Vitest/Vite process; Sol review pending; no push.

Task W3: initial implementation `17c38c3..bd9171d` — review-only capture/upload route behind the sole W2 switch, exact browser timing/MIME guidance, fake C2 upload, and production isolation. Sol review found 1 Critical, 5 Important, and 2 Minor: React StrictMode left `mountedRef=false` after effect replay and broke real capture/upload; preview did not autoplay; default fake upload collapsed preparation/progress/cancel; capture mutations raced active upload; chunks retained local media; source MIME/size metadata was rewritten/rounded; accepted handoff left stale preview state; transformed harness omitted capture evaluation marker. Fix round 1 resumed with original Terra xhigh; no push.

Task W3: fix round 1 `1b2f761..cdac76b` — lifetime replay, live preview, phase-based cancelable fake upload, upload generation/controller race guards, full chunk/media cleanup, source-vs-wire metadata and exact bytes, truthful accepted state, and capture marker proof all addressed with component and real-browser regressions. Sol re-review: Spec PASS, Quality Approved, CLEAN; focused 34/34, StrictMode Playwright desktop/mobile, production artifact 4/4, lint/typecheck/build, root check, and controller fresh gates passed. Awaiting push and hosted CI.

Task W3: complete at `cdac76b` — hosted run `33623502112` passed quality in 9m23s (`pnpm check`, production-router 4/4, canonical Linux visuals, OpenAPI, focused operability) and clean executable/operability probes in 3m05s. Local/remote main equal, worktree clean, Terra implementation plus Sol final review CLEAN.

Task W4: initial implementation `37660ac..307eeb1` activated sole production `/verified` owner, calibrated challenge tracer, upload/polling/report, and live/frozen ranking. Sol review found camera lifetime, setup/capture parity, create/upload/polling races, incomplete provenance, leaderboard staleness, and accessibility gaps. Three fix rounds with the original Terra produced `5f7e665..6984d85`: neutral shared setup/capture models, real production camera, XHR byte progress, authoritative upload reconciliation, generation guards, full ranked provenance/snapshot, accessible ranking, and regressions for cancel/duplicate/transport ambiguity/Back. Same Sol final review: all 10 original findings plus 2 residual findings RESOLVED; Spec PASS, Quality Approved, CLEAN. Controller final gates: 157/157 focused, production browser 6/6, root check green with 193 web tests and 24 structural visuals plus 8 expected skips. Awaiting push and hosted CI.

Task W4: complete at `6984d85` — hosted run `33638890068` passed quality in 9m12s (`pnpm check`, production-router 6/6, canonical Linux visual gate, OpenAPI, focused operability) and clean executable/operability probes in 3m01s. Local/remote main equal, worktree clean, Terra implementation plus Sol final review CLEAN.

Task W5: started from `6984d85`; manual brief matched to current W5 plan and MVP Free contracts. Fresh Terra xhigh owns implementation; fresh Sol xhigh review follows functional/report commits.

Task W5: implementation `6078566..8cb14d5` completed after seven review rounds with the original Terra and same Sol reviewer. Final scope includes sole `/free-training` owner, exact approximate Free insight, shared upload/poll lifecycle, destructive History states, causal `Idempotency-Key` replay with SQLite migration v23 and athlete-scoped uniqueness, fail-closed storage health, tombstone replacement, and serialized correlated cleanup. Sol final review: Spec PASS, Quality Approved, CLEAN. Controller final root check passed: web 279, API 492 plus one expected skip, structural browser 24 plus 8 expected skips, production browser 23/23, builds 7/7. Awaiting push and hosted CI.

Task W6: Ruling: approved `mobile-processing.png` notification promise is non-normative because the MVP forbids closed-app notification claims; match composition while showing foreground/manual-refresh truth. Approved `mobile-report.png` is a ranked state, so compare it only through the separate policy-approved mocked API fixture with `Resultado validado — vale para ranking`; the real demo HTTP report remains visibly demo/noncompetitive. Cost if wrong: visual mismatch increases, but product truth and W6 ranked-fixture separation remain binding.

Task W5: complete at `8cb14d5` — hosted run `33666854310` passed quality in 9m54s (`pnpm check`, production-router, canonical Linux visual gate, OpenAPI, focused operability) and clean executable/operability probes in 2m53s. Local/remote main equal, worktree clean, Terra implementation plus Sol final review CLEAN.

Task W6: started from `8cb14d5`; manual brief binds real demo HTTP Free/verified flows, isolated ranked fixture, complete approved-reference overlay/diff matrix, truth-over-mockup rulings, committed QA evidence, and independent acceptance. Fresh Terra xhigh implementation; fresh Sol xhigh review follows.

Task W6: initial implementation `eabf3ff..5e46a75` delivered real demo browser coverage, ranked fixture isolation, full reference artifact pipeline, CI wiring, and QA evidence; root check passed. Implementer self-audit correctly kept W6-V02..V06 open as P0 because production Verified states remain visually divergent. Pre-review remediation returned to same Terra: selected screenshots are approved image-to-code targets; implement challenge/setup/capture/pending/ranked compositions with approved assets and truth constraints, then recapture until no actionable P0/P1/P2. No Sol dispatch yet.

Task W6: pre-review remediation `b20bf02..b71abc2` implemented public challenge choice plus calibrated/capture/pending/ranked compositions and refreshed unmasked overlay/diff evidence. Terra reports 63 focused, production-router 23, demo E2E 2, Darwin visual 29, and root check green; all prior P0/P1/P2 self-closed with approved-asset/truth variance classified P3. Fresh Sol xhigh task review receives full `8cb14d5..b71abc2` package and must independently inspect every reference/capture/overlay/diff before acceptance.

Task W6: review at `b71abc2` — Spec NEEDS FIXES, Quality Needs fixes. Open: Critical non-home visual harness accepts any screenshot (`maxMismatchRatio=1`) and V03/V04 exact captures overflow/crop; Important real portrait/verified media replaced by check facts, W6 accessibility evidence overclaimed, `Em breve`/file-picker keyboard semantics violate access rules, and V02 omits truthful `3 metros`/has non-asset layout drift. Minor deferred: duplicate/superseded verified CSS plus oversized `styles.css`/`tracer.tsx`; report commit table omits `5e46a75`. Fix round 1/5 resumes original Terra xhigh from review base `b71abc2`; no push.

Task W6: fix round 1 `94a13e6..c63dada` — static per-state visual budgets plus viewport landmarks/negative proofs, exact V03/V04 bounds at scroll zero, native disabled/file controls, truthful `3 metros`, mobile browser access/error/state evidence, and normal codec media generation with separate check-mode smoke. Terra reports root check green (Web 286), W6 browser 2/2, Darwin visual 30 passed/10 skipped, smoke 2/2; local normal codec run remains honestly blocked by absent FFmpeg and CI provisions it. Same Sol xhigh re-review receives scoped `b71abc2..c63dada`; no push.

Task W6: re-review at `c63dada` — Spec NEEDS FIXES, Quality Needs fixes. Addressed: V03/V04 exact viewport/scroll. Still open: V02–V04 pixel budgets accept material non-hero geometry drift because landmarks only prove containment; clipped file input remains an invisible Tab stop; QA overclaims closure; one non-home budget comment is stale. Codec normal/smoke wiring is correct but hosted normal-runtime green remains required. Fix round 2/5 returns to same Terra xhigh; after local findings pass Sol, controller will push candidate for hosted codec/canonical evidence and return results to Sol.

Task W6: fix round 2 `726ffc6..168f16a` — V02–V04 aligned to selected mobile geometry; per-state source-relative headline/card/visual/action ranges and position/size mutation proofs added; hidden file input removed from Tab/accessibility order with next-visible-control browser proof; QA/status and stale comment corrected. Terra reports root check, approved matrix, structural, and Darwin gates green; hosted codec/canonical remain pending. Same Sol xhigh receives scoped `c63dada..168f16a`; no push.

Task W6: re-review at `168f16a` — hidden file-input Tab/accessibility and QA overclaim addressed. Still open: V03/V04 geometry ranges permit underscaled headlines and excessive gaps; V02 descriptive copy now overlaps hero; state-specific gap/height negative proofs missing; non-home enforcement comment still inaccurate. Codec candidate not push-ready until these local visual findings close. Fix round 3/5 returns to same Terra xhigh; no push.

Task W6: fix round 3 `62f1072..2311342` — V02 copy constrained to source-like three-line white-column wrap; V03/V04 headline scale and visual/preview rhythm aligned; independent height/bottom/gap ranges plus state-specific negative proofs added; harness enforcement comment corrected. Terra reports 8/8 geometry gates, 21/21 focused, approved matrix 2 passed/2 skipped, structural 26 passed/14 skipped, root check green. Same Sol xhigh receives scoped `168f16a..2311342`; no push.

Task W6: re-review at `2311342` — all locally reviewable round-3 findings ADDRESSED; Sol independently inspected V02–V06 artifacts, found no new breakage, returned Spec PASS / Quality Approved, and declared `READY TO PUSH AS HOSTED CI ACCEPTANCE CANDIDATE`. Hosted normal-codec and canonical Linux/x64 evidence remain pending. Controller fresh local gates precede push.

Task W6: CI fix round 4 `c497535` — hosted run `33689431803` exposed an asynchronous test race only: the capture heading could mount before its existing passive focus effect ran, so the old immediate assertion intermittently observed `body` focus. The test now awaits the real eventual focus condition without an application delay or custom timeout. A capture-only focus-effect mutation fails the new assertion, the focused regression passed in 10 fresh CI-mode processes, full tracer is 43/43, and root `pnpm check` is green. Production UI/W0/visual artifacts are unchanged; codec-backed normal-runtime and canonical Linux/x64 acceptance remain controller-hosted gates, and final Sol acceptance remains pending.

CI API FFmpeg recovery: the same hosted run `33689431803` also failed only `local-frame-extraction`'s real codec smoke. Hosted stack evidence located the rejection at strict `isJpeg` validation, not process completion, runner bounds, evidence caps, timestamp/scene selection, file count, or JPEG size. An Ubuntu 24.04 replay with workflow-installed FFmpeg 6.1.1-3ubuntu5 produced 640 files, 640 showinfo and 600 scene records, 38,220 B stdout, 253,403 B stderr (under the 343,680 B C5 cap), and a 5,621-B JPEG whose optional `Lavc…` COM marker the intentionally strict C5 parser rejects. Functional commit `a56c0ca` locks the owned image stream to `mjpeg`, full-range `yuvj420p`, and `-bitexact`, yielding comment-free baseline JFIF without enlarging a bound or relaxing JPEG evidence. The smoke gate now probes all owned filters plus MPEG-4 and MJPEG encoders. RED argv-contract test preceded the change; five focused GREEN runs, strict Ubuntu JPEG decode/full replay, API lint/typecheck/format/diff checks, and full API suite (41 files, 493 passed, one honest absent-host-FFmpeg skip) passed. Detailed evidence: `ci-api-ffmpeg-report.md`. Fresh hosted CI remains required after controller push.

CI API FFmpeg review correction: Sol found the capability claim overbroad and the argv assertion insufficiently ordered. Commit `03c84ce` replaces inventory parsing with a small real capability probe: lavfi/color → MPEG-4/MP4 → decode through the owned filters → bitexact `yuvj420p` MJPEG image2 plus null output, requiring a real readable frame. Its RED test uses an executable that reports every former inventory capability but cannot run FFmpeg; old helper returned true, replacement returns false. The JPEG output assertion is now the exact contiguous five-token encoder block immediately before the image path; its RED state caught `-y` between the block and path, so the existing global `-y` moved before it. Focused suite is 14 passed/1 honest local FFmpeg skip; API lint/typecheck/format/diff checks passed; full API is 41 files, 494 passed/1 skip. Report updated; fresh hosted CI still gates release.

CI API FFmpeg timeout recovery: hosted retry `33693218189` confirmed the prior JPEG COM rejection is fixed, but its real capability probe + 64-second fixture + 640-frame smoke crossed Vitest's implicit 5 s budget at 5,071 ms. `afterEach` then raced the still-writing FFmpeg process and raised `ENOTEMPTY` removing the owned `.frames` directory. Commit `19bda2b` confines a 15 s integration budget to that smoke (below the production 30 s extraction bound), tracks every smoke child, and makes cleanup TERM, await `close`, then remove roots; the 1 s SIGKILL escalation matches the production runner instead of sleeping. RED lifecycle test observed the old untracked child; GREEN proves tracked termination settles before root removal. Ubuntu 24.04 at one CPU measured the real mini-probe 68 ms and full 640-file path 4,473 ms. Focused API 15 pass/1 honest absent-FFmpeg skip, API lint/typecheck/format, and full API 41 files/495 pass/1 skip are green. Detailed evidence: `ci-api-ffmpeg-report.md`; hosted CI remains the final codec-path confirmation.

CI API FFmpeg teardown review correction: Sol found `19bda2b` still released a child on `error` before `close` and drained only a snapshot, so A completion could register B during teardown after the snapshot. Commit `3d549b8` uses a fresh per-test generation tracker that closes registration before draining, immediately terminates late registrations, and loops until empty. Only actual child close can clear forced-kill escalation, release tracking, settle the result, and resolve the close wait; result settlement comes first so A's continuation can register B before the drain resumes. RED/Green regressions cover kill-error-before-close and A-success→B-during-teardown, each proving root removal waits for every close. Focused API 17 pass/1 honest absent-FFmpeg skip plus lint/typecheck/format passed. One full API attempt found unrelated SQLite migration `database is locked`; its exact test passed twice in fresh focused runs and the fresh full API run is green: 41 files/497 pass/1 skip. Detailed report updated; hosted CI remains the final codec-path confirmation.

CI API FFmpeg admission correction: Sol round 4 found `3d549b8` still spawned B before registering it, so an asynchronously late successor could begin after draining observed empty and race root cleanup; its one-time error listener also left a later SIGKILL error unhandled after a TERM error. Commit `bd57535` reserves admission synchronously before spawn and rejects a closed generation without invoking its spawner (releasing the reservation on spawn throw). It keeps the error listener through close, retains the first error, and handles TERM-error→SIGKILL-error→close without early tracker release; only the test injects a 1 ms grace, while the normal 1 s bound remains. RED/Green delayed B until after drain returned and proved its spawner was never called; the multi-error regression proved both events are handled and release only happens on close. Focused API 19 pass/1 honest absent-FFmpeg skip, lint/typecheck/format, and a fresh full API 41 files/500 pass/1 skip are green. Detailed report updated; hosted CI remains the final codec-path confirmation.

CI SQLite startup lock recovery: that earlier one-off migration lock had a real race. `01c0814` correctly delayed persistent WAL activation until after invalid-predecessor validation, but placed it after `applyMigrations` committed. A concurrent starter could take `BEGIN IMMEDIATE` in that gap and make the first connection's `journal_mode = WAL` throw `SqliteError: database is locked`. Functional commit `2c169d9` preserves invalid-file byte safety by running the existing read-only validation first, enables WAL before the migration writer can commit, then keeps the existing under-lock revalidation. A real SQLite worker plus `SharedArrayBuffer` makes the exact post-commit race deterministic: old ordering REDs in 46 ms with the exact error; new ordering GREENs. Invalid-predecessor bytes, fresh/v20 four-child contention, 10 repeated contention runs, API lint, and full API (41 files/499 passed/1 expected skip) pass. API typecheck is temporarily blocked only by concurrent uncommitted FFmpeg test errors in `local-frame-extraction.test.ts`; no push. Detailed evidence: `ci-sqlite-lock-report.md`.

CI SQLite startup TOCTOU correction: Sol found that `2c169d9` still left preflight validation outside the durable WAL transition. Commit `69d5d00` replaces that gap with a temporary exclusive bootstrap connection: validate under `locking_mode=EXCLUSIVE`, enter WAL, close it, then reopen the ordinary connection for under-lock migration validation. This prevents another process from changing history between preflight and WAL without leaking exclusive locking to repositories. A real-worker boundary test proves `user_version=24` is rejected with `database is locked` at the exact WAL boundary. A second real-worker handoff test writes `user_version=24` immediately after the bootstrap releases, proves normal startup fails closed with `sqlite migration history is invalid`, and proves the durable WAL mode/file bytes remain exactly the worker's post-write snapshot. A restore-to-DELETE alternative was rejected because SQLite header counters changed even after the journal mode was restored. Focused migration tests 5/5, five repeated four-child runs, lint, Prettier, and full API 41 files/502 passed/1 expected local FFmpeg skip are green; API typecheck remains blocked only by concurrent FFmpeg errors at `local-frame-extraction.test.ts:1261` and `:1282`. No push. Detailed evidence: `ci-sqlite-lock-report.md`.

CI SQLite DELETE-mode concurrent-start correction: Sol found `69d5d00`'s bootstrap still used `locking_mode=EXCLUSIVE` before acquiring a transaction. Two synchronized starters each retained a SHARED read lock through validation and SQLite selected an immediate `SQLITE_BUSY` loser during the WAL upgrade. Commit `f9fde08` acquires `BEGIN EXCLUSIVE` first, selects `locking_mode=EXCLUSIVE` inside that transaction, validates, commits while retaining the temporary connection policy, enters WAL, closes, and reopens normal. An isolated real-SQLite probe showed that the literal mode-before-begin order also produces an immediate loser; begin-then-mode serializes both. The deterministic two-worker regression aligns both starters immediately before their real exclusive begin and requires both v23 opens/23 ledger rows plus `locking_mode=normal` on each exposed handle. It REDs on the predecessor with exact `SqliteError: database is locked` in 271 ms. Existing invalid-predecessor byte, TOCTOU, post-bootstrap durable-handoff, and migration-WAL-window tests remain green. Focused migration 6/6 and five repeated two-test contention runs pass; format, lint, typecheck, and full API 41 files/503 passed/1 expected FFmpeg skip pass. No push. Detailed evidence: `ci-sqlite-lock-report.md`.

CI API FFmpeg bounded smoke correction: hosted run `33700036063` timed out only the real codec smoke at 15,000 ms, then C5 body cleanup and `afterEach` root deletion raced (`ENOTEMPTY`). Investigation ruled out test-worker concurrency: CI uses one API Vitest worker and root Turbo test concurrency one. The test itself was oversized—64 seconds/1280x720, 640 decoded JPEGs, then 640 C5 materializations—rather than a production C5 timeout failure. Functional commit `9d1875e` retains a real full owned-argv proof but uses a truthful compact Free fixture: 4.05 seconds, 480x480, 12fps MPEG-4; C5 runs fps/split/showinfo/select/metadata, bitexact `yuvj420p` MJPEG image2, and null scene mapping, requires 41 decoded boundary files/4.0 scene evidence, and publishes/validates 12 Free frames. Its new internal 30-second C5-matching deadline closes and drains the per-test child tracker in `finally`. One-CPU Ubuntu FFmpeg 7.1 raw exact-pipeline stress passed 10/10 (41 files incl. index 40 and the 4.0 scene). Detailed evidence: `ci-api-ffmpeg-report.md`.

CI API FFmpeg finite-backstop correction: Sol found that the `9d1875e` smoke had disabled its only independent test bound and could let an expired capability probe propagate Vitest's pending/skip outcome. Functional commit `a3b5a23` retains the 30 s C5-matching inner deadline but uses a scoped 32 s Vitest backstop (30 s plus the owned 1 s TERM and 1 s SIGKILL grace), with no global or production timeout change. It captures a per-test tracker/body settlement and keeps the smoke root private. On backstop, `afterEach` allows one owned grace for tracker drain and one for body settlement; a miss fails explicitly and preserves roots rather than deleting against a live continuation. The wrapper overrides both post-expiry results and rejections with its owned deadline error. RED/GREEN coverage models a terminated capability probe returning false then a `PendingError`-shaped skip, and a deliberately un-settled body that must leave its root readable after teardown failure. The real smoke now asserts FFmpeg exit zero, exactly decoded indices 0..40/no 41, and parsed scene metadata `[4]` before C5 publishes 12 readable Free frames. Focused API is 22 pass/1 honest absent-FFmpeg skip and passed three fresh sequential processes; API lint/typecheck/focused Prettier/diff checks passed. A fresh Ubuntu FFmpeg 7.1 one-CPU exact pipeline completed its 41-index/4.0 boundary proof. Fresh workflow FFmpeg 6.1 hosted CI remains required. No push.

Task W6: CI demo-E2E fix `ef2ff28` — hosted `33703583459` passed root check/production-router then exited before normal API readiness. Exact cause: private `REVELAI_DEMO_E2E` crossed into strict API env validation and was rejected before bind. Harness now strips only that marker, preserves normal CI/non-REVELAI config, classifies `EADDRINUSE` safely, and moves runner smoke to 4176 with 4174-separation plus post-run ownership tests. Controlled collision reproduced old generic symptom but no hosted orphan is claimed. Docker Ubuntu+FFmpeg reaches real V05 processing; terminal throughput is Rosetta `node --no-opt` limited, so native hosted codec is the controller gate. API smoke, Web 13 Node + 291 Vitest + structural 26/14, Web lint/typecheck/format/diff green. No push; Sol acceptance pending.

Task W6: CI P1 remediation `e6aa4fc` + `e858e51` — real Verified browser trace now asserts progress → real pending heading/enabled manual refresh → terminal, not progress alone. Demo wrapper readiness is bound to the spawned API's post-listen stdout marker and abortable child-exit-raced health probe, so a foreign health-200 owner or hanging socket fails safely; its own fatal message does not expose absolute paths. Real-wrapper regressions 3/3, smoke browser traces 2/2, API smoke, env isolation 1/1, runner ownership 6/6, Web lint/typecheck/format/diff all green. Native Linux/x64 codec terminal and Sol acceptance remain pending; no push.

Task W6: lifecycle re-review `e99733f` — wrapper now discards all owned child output except an internal exact readiness token; pre-setup stack/path/sentinel regressions prove no raw child disclosure. TERM has a named 1 s grace then SIGKILL solely for the spawned child and waits for `close`; normal and resistant-child wrapper runs prove both 4174/4175 free afterward. Verified browser assertions share one 120 s post-upload deadline across progress/pending/terminal; 150 s is teardown/reporting backstop only. Wrapper suite 6/6, smoke browser 2/2, API smoke, env 1/1, runner ownership 6/6, Web lint/typecheck/format/diff green. Native Linux/x64 terminal and Sol acceptance remain pending; no push.

Task W6: historical hosted acceptance at `12318cb43a9669f67caa9eb462cbfd1f48a69f3d` — independent Sol approved [workflow 33712809662](https://github.com/ToledoVitor/revelAI/actions/runs/33712809662) at that checkpoint after every gate exited 0: `pnpm check` in about 7m31s within the 12m14s Demo-only quality checks job; production-router 23/23; normal real-FFmpeg/C10 demo E2E Node 18/18 plus browser 2/2 through terminal; canonical Linux/x64 visual 30 passed/10 skipped; focused API 23/23; OpenAPI; and operability 18/18 within the 2m54s clean-executable/operability job. The accepted V05 foreground/manual-refresh and V06 isolated-ranked-fixture truth rulings are unchanged.

Task W6: hosted revalidation pending after `33714558200` failed only Node lifecycle test 18 at 643ms (tests 1–17 green): a generic first-codec marker let SIGTERM land before the second delayed codec admitted, so the test observed one close instead of its correct two. Commit `2e04bee` adds distinct `free-portrait.mp4`/`verified-landscape.mp4` start and close markers and waits for both before signal; follow-up `acf1faa` installs both SIGINT/SIGTERM handlers before publishing `:handlers-ready`. The barrier keeps the two-close assertion, no API marker, media cleanup, and port ownership proof. RED: the two-marker test fails against the generic predecessor. GREEN: 16 fresh handlers-armed focused processes, full Node 18/18, browser smoke 2/2, lint/typecheck/Prettier/diff. W6 is DONE_WITH_CONCERNS while hosted revalidation remains pending; independent Sol and a new native hosted green remain required.
